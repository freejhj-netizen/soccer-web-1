# 상대팀 분석 데이터 복구 가이드

## 데이터 복구 가능 여부

Firestore는 삭제된 데이터를 자동으로 복구할 수 없습니다. 하지만 다음 방법으로 확인할 수 있습니다:

### 방법 1: Firebase Console에서 확인

1. **Firebase Console 접속**
   - https://console.firebase.google.com/
   - 프로젝트 선택: `webpage-2a5e5`

2. **Firestore Database 확인**
   - Firestore Database → 데이터 탭
   - `opponentAnalyses` 컬렉션 확인
   - 데이터가 있는지 확인

3. **데이터가 있다면**
   - 페이지를 새로고침하거나 캐시를 지우고 다시 확인
   - 코드의 컬렉션 이름이 올바른지 확인

### 방법 2: Firebase 백업 확인

Firebase는 자동 백업을 제공하지 않지만, 다음을 확인할 수 있습니다:

1. **Firebase Console → Firestore Database → 백업**
   - 백업이 설정되어 있다면 복구 가능
   - 백업이 없다면 수동 복구 불가

### 방법 3: 수동 복구 (데이터가 완전히 삭제된 경우)

데이터가 완전히 삭제되었다면 복구가 불가능합니다. 다음을 확인하세요:

1. **다른 관리자 계정으로 확인**
   - 다른 관리자 계정으로 로그인하여 데이터 확인

2. **브라우저 캐시 확인**
   - 브라우저 캐시를 지우고 새로고침
   - 시크릿 모드에서 접속하여 확인

## 권한 오류 해결

현재 "Missing or insufficient permissions" 오류는 Firestore 보안 규칙의 컬렉션 이름 불일치로 인한 것입니다.

**해결 방법:**
1. Firebase Console → Firestore Database → 규칙 탭
2. `firestore.rules` 파일의 내용을 복사하여 붙여넣기
3. "게시" 버튼 클릭

또는 Firebase CLI로 배포:
```bash
firebase deploy --only firestore:rules
```

## 예방 방법

향후 데이터 손실을 방지하기 위해:

1. **정기적인 백업**
   - Firebase Console에서 수동으로 데이터 내보내기
   - 또는 Firebase Admin SDK를 사용한 자동 백업 설정

2. **권한 확인**
   - 관리자만 삭제 권한을 가지도록 설정 확인
   - 삭제 시 확인 팝업 추가

3. **로깅**
   - 데이터 변경 사항을 로그로 기록


